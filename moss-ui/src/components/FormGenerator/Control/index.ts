import MyCheckbox from './CheckBox';
import Radio from './Radio';
import RangePicker from './RangePicker';
import Select from './Select';

import Transfer from './Transfer';

const myControl: { [key: string]: any } = {
  MyCheckbox,
  Radio,
  RangePicker,
  Select,
  Transfer,
};

export default myControl;
